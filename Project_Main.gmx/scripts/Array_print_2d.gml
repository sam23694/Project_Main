//Array_print_2d(array, length, width)

var tstr;

for(var i = 0; i < argument1; i++)
{
    for(var j = 0; j < argument2; j++)
    {
        tstr += string(argument0[i, j])+", ";
    }
    tstr += "#";
}
